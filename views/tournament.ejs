<div class="homepage">
  <div class="header">
    <h1><%= teams.platform + " " + teams.gameType%> Tournament</h1>
  </div>
</div>
<div class="tournament-view">
    <h1>Tournament</h1>
    <% teams.teams.forEach(function(team){ %>
        <p class="team"><%= team.name %></p>
    <% }); %>
</div>